Great detailed list. Paste this entire prompt into Replit AI chat:

"I need 11 design and functionality fixes to get the site launch-ready. Please implement all of them:
1. Landing page email capture — change the copy from whatever it currently says to: 'Create a free account to add wines to your watchlist and get alerted when they drop on LCBO Vintages.' The CTA button should say 'Create Free Account' and clicking it goes to /signup, not just capturing an email.
2. Landing page tagline — replace the current subtitle with: 'Thursday Drop tracks every LCBO Vintages release and sends you a personal alert the moment your favourite wines become available. Never miss an allocation again.'
3. Remove the latest release table from the landing page entirely. The landing page should be clean — hero section, how it works, pricing preview, that's it. No wine table on the homepage.
4. Fix the top navigation spacing. Look at the screenshot — Release and History are bunched in the center-left, then there is a huge gap, then a divider line, then Watchlist, Account, Sign Out are on the far right. Fix it so all right-side links (Watchlist, Account, Sign Out) are evenly spaced with consistent gaps between them, no divider line between them, and the whole right group sits flush right. The center links (Release, History) should be truly centered. Remove the vertical divider line between the two groups.
5. Re-run the scraper right now against the live LCBO website to pull fresh data for all three sections — Special Offers, Monthly Collection, and Bordeaux Futures. The Special Offers page should be showing all current offers including Baettig, Louis Latour, Perrier-Jouet, Muga, Barolo and Barbaresco, Edouard Delaunay, and Bordeaux Gems. After re-scraping show me the count of wines found per program.
6. History page — add a search bar at the top that searches across wine name, producer, and region in real time. Add pagination showing 50 wines per page with Previous and Next buttons at the bottom.
7. History page filters — restructure the filter hierarchy to: first filter by Year (2026, 2025 etc), then within the year filter by program type (Special Offers, Monthly Collection, Bordeaux Futures), then within that show the individual drops from that year and type. This should work as a cascading filter — selecting a year populates the program type options, selecting a program type populates the individual drop options.
8. Sign out confirmation — when a user clicks Sign Out in the nav, show a confirmation modal that says 'Are you sure you want to sign out?' with two buttons: Yes, Sign Out and Cancel. Only sign out if they click Yes.
9. Release page — all program sections should be collapsed by default when the page loads. Users must click a program name to expand it and see the wines underneath. Currently they are all expanded on load which is overwhelming. Make sure only one can be expanded at a time.
10. Account page pricing cards — the Subscribe Monthly and Subscribe Annual cards must be perfectly vertically aligned. They should be the same height, same top position, same bottom position. Use align-items: stretch on the flex container so both cards are always equal height regardless of content.
11. General launch readiness — do a full audit of the site and fix any obvious errors, broken links, missing states, or UI inconsistencies. Specifically check: empty states on all pages show helpful messages, all buttons do something when clicked, no console errors, mobile layout is reasonable, all forms validate input before submitting, loading states show while data is fetching.